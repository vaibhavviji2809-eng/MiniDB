[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "minidb"
version = "0.1.0"
description = "MiniDB educational database engine"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["MiniDB"]

