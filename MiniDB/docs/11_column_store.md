# Column Store

MiniDB supports row-store and column-store table layouts.

Column store tables keep values grouped by column, which can be useful for analytical queries and projection-heavy workloads.

